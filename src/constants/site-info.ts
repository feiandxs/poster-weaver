export interface SiteInfoSchema {
  title: string;
  description: string;
}

const siteInfo: SiteInfoSchema = {
  title: 'Poster Weaver',
  description: 'Generate Poster Quickly',
};

export { siteInfo };
